<section class="section">
	<h2>
		Handle Access 
	</h2>
	<div class="section-content-row">
		<div class="content-box">
			<p>
				Finalize the Instance HUB Registration

				{{ form_start(form, {					
					method: 'POST',
				}) }}

				{{ form_row(form._token) }}
				{# ensures CSRF token is included #}
				<button class="business-btn" type="submit">The HUB - Frontend registration is done.</button>

				{{ form_end(form) }}
			</p>
		</div>
	</div>
</section></div>
